# Provision Build Configuration

# Configurable Settings
# -------------------
BASE_IMAGE="nvflare_base_image.qcow2"
OUTPUT_IMAGE="nvflare_provisioned.qcow2"
SAFETY_FACTOR=2.0         # Multiply package size by this for partition sizing

# Fixed Settings (DO NOT MODIFY)
# -----------------------------
# Fixed Package Paths (determined by base image)
APP_PEX_PATH="/host/cc/packages/app.pex"
STARTUP_KIT_PATH="/host/cc/packages/startup_kit.tar.gz"

# Fixed Installation Paths (determined by base image)
APP_INSTALL_PATH="/etc/cc/apps"
STARTUP_INSTALL_PATH="/host/cc/startup"

# Fixed Partition Numbers (determined by base image)
APP_PARTITION_NUM=4        # CC Apps partition (p4)
CONFIG_PARTITION_NUM=2     # Host-visible config partition (p2)

# Build Settings
# SAFETY_FACTOR=2.0         # Multiply package size by this for partition sizing 