# Encryption patterns for IO interceptor

# Read-write encryption patterns (both encrypt and decrypt)
ENCRYPT_RW_PATHS=/workspace/models/*.pt,/workspace/checkpoints/*.ckpt,/workspace/state/*.state

# Write-only encryption patterns (encrypt only)
ENCRYPT_WO_PATHS=/var/log/nvflare/*.log,/workspace/metrics/*.json 