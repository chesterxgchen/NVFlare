# FL Communication ports (comma-separated)
FL_PORTS="8002,8003"  # FL communication, Admin API

# Attestation Services Configuration
# Format: port:protocol:vendor (tcp or udp)
ATTESTATION_PORTS="9000:tcp:intel,  # Intel ITA (Trust Authority)
                    9001:tcp:amd,     # AMD SNP Guest
                    9002:tcp:azure,    # Microsoft Azure Attestation
                    9003:tcp:google,   # Google Cloud Attestation
                    9004:tcp:nvidia"   # NVIDIA Attestation

# Attestation service networks (comma-separated CIDR)
ATTESTATION_NETWORKS=""  # Must be configured per environment

# Monitoring Configuration
# Format: port:protocol (tcp or udp)
MONITOR_PORTS="9090:tcp,9102:tcp,8125:udp"  # Prometheus, statsd-exporter, statsd
MONITOR_NETWORK=""  # Must be configured, e.g. "10.0.0.0/8"

# Allowed networks (comma-separated CIDR notation)
ALLOWED_NETWORKS=""

# Connection limits
MAX_CONNECTIONS_FL=20
MAX_CONNECTIONS_ADMIN=5
MAX_CONNECTIONS_MONITOR=5
MAX_CONNECTIONS_ATTESTATION=10

# Rate limiting
RATE_LIMIT="100/minute"
ATTESTATION_RATE_LIMIT="60/minute"  # Attestation request rate limit

# System service configuration
DISABLE_SSH=true      # Disable SSH access
SSH_BACKUP=true      # Backup SSH keys before removal

# FL Communication ports (comma-separated)
FL_PORTS="8002,8003"  # FL communication, Admin API

# Monitoring Configuration
# Format: port:protocol (tcp or udp)
MONITOR_PORTS="9090:tcp,9102:tcp,8125:udp"  # Prometheus, statsd-exporter, statsd
MONITOR_NETWORK=""  # Must be configured, e.g. "10.0.0.0/8"

# Allowed networks (comma-separated CIDR notation)
ALLOWED_NETWORKS=""

# Connection limits
MAX_CONNECTIONS_FL=20
MAX_CONNECTIONS_ADMIN=5
MAX_CONNECTIONS_MONITOR=5
MAX_CONNECTIONS_ATTESTATION=10

# Rate limiting
RATE_LIMIT="100/minute"
ATTESTATION_RATE_LIMIT="60/minute"  # Attestation request rate limit 