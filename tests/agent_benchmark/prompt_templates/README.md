# Agent Benchmark Prompt Templates

Prompt parameterization will live here when scenario-driven workflow rendering
is implemented. The migrated harness currently keeps the original stable prompt
contract in `../prompts/benchmark_prompt.txt`.
