# Agent Benchmark Prompt Templates

Prompt parameterization will live here when scenario-driven workflow rendering
is implemented. The current harness accepts the measured prompt as a runtime
`--prompt PATH` argument and mounts that file into the container.
