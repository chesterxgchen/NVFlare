# Agent Benchmark Fixtures

This directory is reserved for small synthetic job folders, sample agent event
streams, and sample process records used by unit, adapter, and smoke tests.

Do not store real benchmark datasets, ad-hoc run outputs, or large generated
artifacts here.
