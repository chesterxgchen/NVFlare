# Wrong Root Fixture

This folder is intentionally not the submit-ready exported job root. The nested
`nested-exported-job` folder is present so evals can verify that agents report
the requested-path submit failure instead of guessing and resubmitting a nearby
folder.
